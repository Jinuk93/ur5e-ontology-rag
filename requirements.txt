# ============================================================
# requirements.txt - 프로젝트 전체 공통 패키지
# ============================================================
# 설치 명령어: pip install -r requirements.txt
#
# 패키지 버전 표기법:
#   패키지명==1.0.0  → 정확히 이 버전만
#   패키지명>=1.0.0  → 이 버전 이상
#   패키지명~=1.0.0  → 1.0.x 범위 (마이너 업데이트만 허용)
# ============================================================


# ------------------------------------------------------------
# [1] 환경 설정 관련
# ------------------------------------------------------------
python-dotenv>=1.0.0          # .env 파일에서 환경변수 로드
pyyaml>=6.0                   # YAML 파일 읽기/쓰기 (설정 파일용)


# ------------------------------------------------------------
# [2] PDF 처리 관련
# ------------------------------------------------------------
pymupdf>=1.23.0               # PDF 텍스트 추출 (fitz라고도 불림)
# 참고: import fitz 로 사용 (패키지명과 import명이 다름!)


# ------------------------------------------------------------
# [3] AI/LLM 관련
# ------------------------------------------------------------
openai>=1.0.0                 # OpenAI API 클라이언트 (GPT, Embedding)
tiktoken>=0.5.0               # OpenAI 토큰 계산 (비용 추정용)


# ------------------------------------------------------------
# [4] 벡터 DB 관련
# ------------------------------------------------------------
chromadb>=0.4.0               # 벡터 데이터베이스 (유사도 검색)


# ------------------------------------------------------------
# [5] 그래프 DB 관련
# ------------------------------------------------------------
neo4j>=5.0.0                  # Neo4j Python 드라이버


# ------------------------------------------------------------
# [6] 웹 프레임워크 관련
# ------------------------------------------------------------
fastapi>=0.100.0              # API 서버 프레임워크
uvicorn>=0.23.0               # ASGI 서버 (FastAPI 실행용)
pydantic>=2.0.0               # 데이터 검증 (FastAPI와 함께 사용)


# ------------------------------------------------------------
# [7] UI 대시보드 관련
# ------------------------------------------------------------
streamlit>=1.28.0             # 대시보드 UI 프레임워크
plotly>=5.18.0                # 인터랙티브 차트 (Gauge, Bar, Line 등)
pyvis>=0.3.2                  # Neo4j 스타일 그래프 시각화
networkx>=3.2.0               # 그래프 데이터 구조 (pyvis 의존성)
pandas>=2.0.0                 # 데이터 처리 및 테이블 표시


# ------------------------------------------------------------
# [8] 유틸리티
# ------------------------------------------------------------
requests>=2.31.0              # HTTP 요청 라이브러리
tqdm>=4.66.0                  # 진행 표시줄 (긴 작업 시 유용)


# ------------------------------------------------------------
# [9] 센서 데이터 처리 (Main 추가)
# ------------------------------------------------------------
pyarrow>=14.0.0               # Parquet 파일 지원
duckdb>=0.9.0                 # 빠른 SQL 조회
scipy>=1.11.0                 # FFT, 신호 분석


# ------------------------------------------------------------
# [10] 개발/테스트 도구
# ------------------------------------------------------------
pytest>=7.4.0                 # 테스트 프레임워크
black>=23.0.0                 # 코드 포맷터 (자동 정렬)
