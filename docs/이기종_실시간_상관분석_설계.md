# 이기종 실시간 상관분석 시스템 설계

> UR5e 로봇 + Axia80 힘센서 데이터를 결합하여 실시간 위험 감지 및 예측

---

## 1. 현재 상태 및 문제점

### 1.1 현재 데이터 현황

| 장비 | 현재 데이터 | 문제점 |
|-----|-----------|-------|
| **Axia80** | Fx, Fy, Fz (힘 센서) | 시뮬레이션 데이터, 실시간 수치 있음 ✅ |
| **UR5e** | 에러코드만 | 실시간 수치 없음, 사후 이벤트만 ❌ |

### 1.2 왜 문제인가?

```
현재:
  Axia80 (힘) ───────────────────▶ 단독 모니터링만 가능
  UR5e (에러코드) ─────────────────▶ 사후 보고만 가능

  → "왜 힘이 커졌는지" 알 수 없음
  → 예측/예방 보전 불가능
```

**필요한 것:**
```
목표:
  Axia80 (힘) ◀──────상관관계──────▶ UR5e (속도/토크/전류)

  → "고속 이동 중 접촉 → 힘 스파이크" 관계 파악
  → 패턴 기반 예측 가능
  → 예방 보전 가능
```

---

## 2. 해결 방안: 3층 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│  Layer 3: 프레젠테이션                                           │
│  ├─ Live 탭: 이기종 상관분석 테이블                               │
│  ├─ 위험도/예측 시각화                                           │
│  └─ 온톨로지 그래프 연결 (요약된 이벤트만)                         │
├─────────────────────────────────────────────────────────────────┤
│  Layer 2: 분석/파생 엔진                                         │
│  ├─ 상관관계 계산 (correlation_engine.py)                        │
│  ├─ 위험도 점수 산출 (contact_risk_score)                        │
│  ├─ 충돌 위험 점수 (collision_risk_score)                        │
│  └─ 권장 조치 결정 (recommended_action)                          │
├─────────────────────────────────────────────────────────────────┤
│  Layer 1: 데이터 수집/생성                                       │
│  ├─ Scenario Sequencer: 시나리오 상태 머신 (5~20초 구간 유지)     │
│  ├─ Axia80: 시나리오 기반 생성 + 개별 노이즈 + 지연               │
│  └─ UR5e: 시나리오 기반 생성 + 개별 노이즈                        │
│           ※ 역산 방식 X → 시나리오가 원인, 양쪽이 각각 관측       │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 시간/샘플링 정의 ⭐ (신규)

### 3.1 기본 파라미터

| 파라미터 | 값 | 설명 |
|---------|---|------|
| `sampling_rate` | 1 Hz | 데이터 생성/전송 주기 |
| `window_size` | 3초 | 분석 윈도우 크기 |
| `buffer_size` | 60개 | 프론트엔드 버퍼 (최근 60초) |
| `timestamp_format` | ISO8601 | `2024-01-24T10:01:02.000Z` |

### 3.2 시나리오 구간 정의

| 파라미터 | 값 | 설명 |
|---------|---|------|
| `scenario_min_duration` | 5초 | 시나리오 최소 유지 시간 |
| `scenario_max_duration` | 20초 | 시나리오 최대 유지 시간 |
| `transition_smoothing` | 2초 | 시나리오 전환 시 보간 구간 |

### 3.3 노이즈/지연 파라미터

| 파라미터 | Axia80 | UR5e | 설명 |
|---------|--------|------|------|
| `noise_std` | ±5% | ±3% | 기본값 대비 노이즈 |
| `response_delay` | 100~200ms | 0ms | 반응 지연 (힘은 로봇 동작 후 지연) |

---

## 4. 데이터 설계

### 4.1 UR5e 합성 텔레메트리 (새로 생성)

| 필드명 | 타입 | 단위 | 범위 | 설명 |
|-------|------|-----|------|-----|
| `timestamp` | string | ISO8601 | - | Axia80과 동일 시간 |
| `tcp_speed` | float | m/s | 0 ~ 1.0 | 툴 끝단 이동 속도 |
| `tcp_acceleration` | float | m/s² | -5 ~ 5 | 가속도 |
| `joint_torque_sum` | float | Nm | 0 ~ 150 | 6개 관절 토크 합계 |
| `joint_current_avg` | float | A | 0.5 ~ 5.0 | 평균 모터 전류 |
| `safety_mode` | enum | - | normal/reduced/protective_stop | 안전 모드 |
| `program_state` | enum | - | running/paused/stopped | 프로그램 상태 |
| `protective_stop` | bool | - | true/false | 보호정지 발생 여부 |

### 4.2 파생/관계 지표 (계산으로 생성)

| 필드명 | 타입 | 범위 | 계산 방식 |
|-------|------|-----|----------|
| `force_magnitude` | float | 0~500 N | √(Fx² + Fy² + Fz²) |
| `force_rate` | float | N/s | d\|F\|/dt (변화율) |
| `force_spike` | bool | - | \|F\| > 100N |
| `high_speed` | bool | - | tcp_speed > 0.5 m/s |
| `torque_force_ratio` | float | - | joint_torque_sum / max(\|F\|, 1.0) |
| `contact_risk_score` | float | 0~1 | 복합 위험도 점수 |
| `collision_risk_score` | float | 0~1 | 충돌 위험 점수 (확률 아님) |
| `anomaly_detected` | bool | - | 토크↑ but 힘→ (마모 징후) |
| `recommended_action` | enum | - | maintain/slow_down/stop/inspect |

> ⚠️ **주의**: `collision_risk_score`는 "확률"이 아닌 "위험도 점수"입니다. UI에서도 "Risk"로만 표시.

### 4.3 통합 데이터 구조 (API 응답)

```json
{
  "timestamp": "2024-01-24T10:01:02.000Z",
  "scenario": {
    "current": "normal",
    "elapsed_sec": 8,
    "remaining_sec": 7
  },
  "window": {
    "start": "2024-01-24T10:01:00.000Z",
    "end": "2024-01-24T10:01:02.000Z",
    "duration_sec": 2
  },
  "axia80": {
    "Fx": 5.2,
    "Fy": -3.1,
    "Fz": -45.8,
    "force_magnitude": 46.2,
    "force_rate": 12.5,
    "force_spike": false,
    "peak_axis": "Fz"
  },
  "ur5e": {
    "tcp_speed": 0.25,
    "tcp_acceleration": 0.8,
    "joint_torque_sum": 42.5,
    "joint_current_avg": 2.1,
    "safety_mode": "normal",
    "program_state": "running",
    "protective_stop": false
  },
  "correlation": {
    "torque_force_ratio": 0.92,
    "speed_force_correlation": 0.78,
    "anomaly_detected": false
  },
  "risk": {
    "contact_risk_score": 0.23,
    "collision_risk_score": 0.15,
    "risk_level": "low",
    "recommended_action": "maintain",
    "prediction_horizon_sec": 3
  }
}
```

---

## 5. 데이터 생성 아키텍처 ⭐ (수정됨)

### 5.1 핵심 원칙: 시나리오 기반 생성

**기존 (문제 있음):**
```
Axia80 Fz → 역산 → UR5e speed/torque
문제: 상관관계가 0.95+ (너무 완벽해서 가짜처럼 보임)
```

**수정 (자연스러움):**
```
[Scenario Sequencer] ──┬──▶ Axia80 생성기 ──(+노이즈 +100~200ms 지연)──▶ Axia80 데이터
                       │
                       └──▶ UR5e 생성기 ──(+노이즈)──────────────────────▶ UR5e 데이터

결과: 상관관계 0.7~0.85 (자연스러움)
```

### 5.2 Scenario Sequencer (상태 머신)

```python
class ScenarioSequencer:
    """시나리오를 구간 단위로 관리하는 상태 머신"""

    SCENARIOS = {
        "normal": {"weight": 0.70, "duration": (10, 20)},      # 10~20초 유지
        "collision": {"weight": 0.10, "duration": (5, 10)},    # 5~10초 (충돌 시퀀스)
        "overload": {"weight": 0.10, "duration": (8, 15)},     # 8~15초 유지
        "wear": {"weight": 0.05, "duration": (10, 20)},        # 10~20초 유지
        "risk_approach": {"weight": 0.05, "duration": (5, 12)} # 5~12초 (점진적 상승)
    }

    def __init__(self):
        self.current_scenario = "normal"
        self.scenario_start_time = time.time()
        self.scenario_duration = random.randint(10, 20)
        self.transition_progress = 0.0  # 0~1 (보간용)

    def tick(self) -> str:
        """매 틱마다 호출: 현재 시나리오 반환 + 필요시 전환"""
        elapsed = time.time() - self.scenario_start_time

        if elapsed >= self.scenario_duration:
            self._transition_to_next()

        return self.current_scenario

    def _transition_to_next(self):
        """가중치 기반 다음 시나리오 선택"""
        weights = [s["weight"] for s in self.SCENARIOS.values()]
        next_scenario = random.choices(list(self.SCENARIOS.keys()), weights=weights)[0]

        self.current_scenario = next_scenario
        self.scenario_start_time = time.time()
        duration_range = self.SCENARIOS[next_scenario]["duration"]
        self.scenario_duration = random.randint(*duration_range)
```

### 5.3 시나리오별 데이터 생성 규칙

#### 시나리오 1: 정상 작업 (normal) - 70%
```python
# 기본값 (시나리오 중심값)
base = {
    "tcp_speed": 0.2,
    "force_magnitude": 20,
    "joint_torque_sum": 30,
}

# Axia80: 기본값 + 노이즈 + 지연
axia80_force = base["force_magnitude"] * (1 + random.gauss(0, 0.05))  # ±5% 노이즈
# 100~200ms 지연 적용 (이전 UR5e 동작의 결과)

# UR5e: 기본값 + 노이즈 (지연 없음)
ur5e_speed = base["tcp_speed"] * (1 + random.gauss(0, 0.03))  # ±3% 노이즈
ur5e_torque = base["joint_torque_sum"] * (1 + random.gauss(0, 0.03))

# 결과
contact_risk_score = 0.1 ~ 0.2
```

#### 시나리오 2: 충돌/접촉 (collision) - 10%
```python
# 시간에 따른 시퀀스 (5~10초 동안)
phase = elapsed_in_scenario / scenario_duration

if phase < 0.3:  # 접근 단계
    tcp_speed = 0.5 + 0.1 * phase
    force_magnitude = 30 + 20 * phase
elif phase < 0.5:  # 접촉 순간
    tcp_speed = 0.6 → 0.2 (급감)
    force_magnitude = 50 → 150 (스파이크)
elif phase < 0.7:  # 반응 단계
    tcp_speed = 0.1
    force_magnitude = 150 → 80
    protective_stop = True (가능성)
else:  # 복구 단계
    tcp_speed = 0.0 → 0.1
    force_magnitude = 80 → 40

# 노이즈 추가
axia80_force += random.gauss(0, force_magnitude * 0.05)
ur5e_speed += random.gauss(0, tcp_speed * 0.03)
```

#### 시나리오 3: 과부하 (overload) - 10%
```python
base = {
    "tcp_speed": 0.2,           # 정상 속도
    "force_magnitude": 65,      # 높지만 스파이크 아님
    "joint_torque_sum": 75,     # 높은 토크
    "joint_current_avg": 4.0,   # 높은 전류
}
# + 개별 노이즈

contact_risk_score = 0.4 ~ 0.6
recommended_action = "reduce_payload"
```

#### 시나리오 4: 마모 징후 (wear) - 5%
```python
base = {
    "tcp_speed": 0.2,           # 정상 속도
    "force_magnitude": 20,      # 정상 힘
    "joint_torque_sum": 60,     # 비정상 높은 토크! (핵심)
}
# torque_force_ratio = 60 / 20 = 3.0 (정상은 1.5 이하)

anomaly_detected = True
recommended_action = "inspect_joints"
```

#### 시나리오 5: 위험 접근 (risk_approach) - 5%
```python
# 시간에 따라 점진적 상승
progress = elapsed_in_scenario / scenario_duration

tcp_speed = 0.3 + 0.2 * progress      # 0.3 → 0.5
force_magnitude = 25 + 30 * progress  # 25 → 55
collision_risk_score = 0.2 + 0.5 * progress  # 0.2 → 0.7

# 중간에 slow_down 권고 발생
if collision_risk_score > 0.5:
    recommended_action = "slow_down"
```

---

## 6. 상태 전이 규칙 ⭐ (신규)

### 6.1 safety_mode와 다른 필드의 일관성

```python
# protective_stop이 True면 반드시:
if protective_stop:
    safety_mode = "protective_stop"
    program_state = "paused"  # 또는 "stopped"
    tcp_speed = 0.0

# safety_mode별 제약
if safety_mode == "normal":
    tcp_speed: 0 ~ 1.0 허용

if safety_mode == "reduced":
    tcp_speed: max 0.25 제한

if safety_mode == "protective_stop":
    tcp_speed = 0.0 강제
    program_state in ["paused", "stopped"]
```

### 6.2 protective_stop 래칭 및 복구

```python
class ProtectiveStopState:
    """보호정지 상태 관리"""

    def __init__(self):
        self.is_active = False
        self.activated_at = None
        self.recovery_delay = 3.0  # 최소 3초 유지

    def trigger(self):
        """보호정지 발동"""
        self.is_active = True
        self.activated_at = time.time()

    def can_recover(self, current_risk: float) -> bool:
        """복구 가능 여부 판단"""
        if not self.is_active:
            return False

        elapsed = time.time() - self.activated_at

        # 조건: 최소 3초 경과 + 위험도 0.3 이하
        return elapsed >= self.recovery_delay and current_risk < 0.3

    def recover(self):
        """보호정지 해제"""
        self.is_active = False
        self.activated_at = None
```

### 6.3 시나리오 전환 시 보간 (Smoothing)

```python
def interpolate_transition(old_values: dict, new_values: dict, progress: float) -> dict:
    """시나리오 전환 시 급격한 변화 방지"""

    # progress: 0.0 (전환 시작) → 1.0 (전환 완료)
    # 2초 동안 보간

    result = {}
    for key in old_values:
        if isinstance(old_values[key], (int, float)):
            result[key] = old_values[key] + (new_values[key] - old_values[key]) * progress
        else:
            # enum/bool은 중간값에서 전환
            result[key] = new_values[key] if progress > 0.5 else old_values[key]

    return result
```

---

## 7. 예측 로직

### 7.1 위험도 점수 계산 (contact_risk_score)

```python
def calculate_contact_risk_score(data: dict) -> float:
    """0~1 사이의 접촉 위험도 점수 계산"""

    score = 0.0

    # 1. 힘 크기 기여 (40%)
    force_norm = min(data['force_magnitude'] / 150, 1.0)
    score += 0.4 * force_norm

    # 2. 속도 기여 (30%)
    speed_norm = min(data['tcp_speed'] / 0.8, 1.0)
    score += 0.3 * speed_norm

    # 3. 힘 변화율 기여 (20%)
    rate_norm = min(abs(data['force_rate']) / 50, 1.0)
    score += 0.2 * rate_norm

    # 4. 토크-힘 불일치 기여 (10%)
    if data['torque_force_ratio'] > 2.0:
        score += 0.1

    return min(score, 1.0)
```

### 7.2 충돌 위험 점수 (collision_risk_score) - 이름 변경

```python
def calculate_collision_risk_score(history: list[dict], horizon_sec: int = 3) -> float:
    """
    최근 데이터 기반 충돌 위험 점수 계산

    ⚠️ 주의: 이것은 "확률"이 아닌 "위험도 점수"입니다.
    UI에서는 "Risk Score" 또는 "위험도"로 표시하세요.
    """

    if len(history) < 3:
        return 0.0

    # 1. 힘 증가 추세 분석
    forces = [h['force_magnitude'] for h in history[-5:]]
    force_trend = calculate_linear_trend(forces)

    # 2. 속도 유지/증가 여부
    speeds = [h['tcp_speed'] for h in history[-5:]]
    speed_trend = calculate_linear_trend(speeds)

    # 3. 현재 위험도
    current_risk = history[-1]['contact_risk_score']

    # 복합 점수 계산 (확률 아님!)
    score = (
        0.3 * min(force_trend / 20, 1.0) +
        0.2 * min(speed_trend / 0.1, 1.0) +
        0.3 * current_risk +
        0.2 * (1 if forces[-1] > 50 else 0)
    )

    return min(max(score, 0.0), 1.0)
```

### 7.3 권장 조치 결정

```python
def determine_recommended_action(
    contact_risk: float,
    collision_risk: float,
    anomaly: bool
) -> str:
    """위험도에 따른 권장 조치"""

    if anomaly:
        return "inspect_joints"  # 마모 징후 → 점검 권고

    if contact_risk >= 0.8 or collision_risk >= 0.8:
        return "emergency_stop"  # 즉시 정지

    if contact_risk >= 0.5 or collision_risk >= 0.5:
        return "slow_down"  # 감속 권고

    if contact_risk >= 0.3 or collision_risk >= 0.3:
        return "caution"  # 주의

    return "maintain"  # 현상 유지
```

---

## 8. API 설계

### 8.1 새로운 엔드포인트

```
GET /api/sensors/correlation/stream
```

**SSE 스트림으로 이기종 상관 데이터 실시간 전송**

Query Parameters:
- `interval`: 전송 간격 (초, 기본값: 1.0)
- `window_size`: 분석 윈도우 크기 (초, 기본값: 3)

Response (SSE):
```
data: {"timestamp": "...", "scenario": {...}, "axia80": {...}, "ur5e": {...}, "correlation": {...}, "risk": {...}}
```

### 8.2 기존 엔드포인트 확장

```
GET /api/sensors/stream
```

기존 Axia80 데이터에 UR5e 데이터 추가:
```json
{
  "timestamp": "...",
  "Fx": 5.2, "Fy": -3.1, "Fz": -45.8,
  "ur5e": {
    "tcp_speed": 0.25,
    "joint_torque_sum": 42.5,
    "safety_mode": "normal"
  }
}
```

---

## 9. 프론트엔드 설계

### 9.1 Live 탭 UI 구성

```
┌─────────────────────────────────────────────────────────────────┐
│  [RiskAlertBar] - 현재 위험 상태 배너                            │
├─────────────────────────────────────────────────────────────────┤
│  [Entity Cards] - UR5e, Axia80, Fx, Fy, Fz 상태 카드            │
├─────────────────────────────────────────────────────────────────┤
│  [RealtimeChart] - 힘 센서 실시간 차트 (기존)                    │
├─────────────────────────────────────────────────────────────────┤
│  [CorrelationTable] - 이기종 상관분석 테이블 (신규) ⭐           │
│  ┌─────┬────────┬────────┬──────┬──────┬──────┬──────┐         │
│  │시간 │Axia |F││UR5e 속도│ 토크 │상관  │위험도│ 권장 │         │
│  ├─────┼────────┼────────┼──────┼──────┼──────┼──────┤         │
│  │10:01│ 25.3 N │0.21 m/s│ 35Nm │ 0.82 │🟢0.15│ 유지 │         │
│  │10:02│ 85.6 N │0.12 m/s│ 78Nm │ 0.91 │🔴0.82│ 감속 │         │
│  └─────┴────────┴────────┴──────┴──────┴──────┴──────┘         │
├─────────────────────────────────────────────────────────────────┤
│  [EventList] - 최근 이벤트 (기존)                                │
├─────────────────────────────────────────────────────────────────┤
│  [HeterogeneousPrediction] - 이기종 예측 (기존, 연동)            │
└─────────────────────────────────────────────────────────────────┘
```

### 9.2 CorrelationTable 컴포넌트 스펙

```typescript
interface CorrelationRow {
  timestamp: string;
  scenario: {
    current: 'normal' | 'collision' | 'overload' | 'wear' | 'risk_approach';
    elapsedSec: number;
  };
  axia80: {
    forceMagnitude: number;
    forceRate: number;
    forceSpike: boolean;
    peakAxis: 'Fx' | 'Fy' | 'Fz';
  };
  ur5e: {
    tcpSpeed: number;
    jointTorqueSum: number;
    jointCurrentAvg: number;
    safetyMode: 'normal' | 'reduced' | 'protective_stop';
    protectiveStop: boolean;
  };
  correlation: {
    torqueForceRatio: number;
    speedForceCorrelation: number;
    anomalyDetected: boolean;
  };
  risk: {
    contactRiskScore: number;
    collisionRiskScore: number;  // 이름 변경 (probability → risk_score)
    riskLevel: 'low' | 'medium' | 'high' | 'critical';
    recommendedAction: string;
  };
}
```

---

## 10. 온톨로지 연결

### 10.1 원칙: 수치 X, 요약된 사실만

온톨로지 그래프에 time-series 수치를 직접 넣으면 그래프가 폭발함.
대신 **이벤트/상태 요약**만 노드/엣지로 추가.

### 10.2 추가할 노드 타입

```
- TelemetryWindow: 특정 시간 구간의 분석 결과
- ForceSpike: 힘 스파이크 이벤트
- HighSpeed: 고속 이동 상태
- CollisionRisk: 충돌 위험 감지
- WearAnomaly: 마모 징후 감지
- ProtectiveStop: 보호정지 발생
```

### 10.3 추가할 관계 타입

```
TelemetryWindow ──HAS_FEATURE──▶ ForceSpike
ForceSpike ──CORRELATED_WITH──▶ HighSpeed
ForceSpike ──INDICATES──▶ CollisionRisk
CollisionRisk ──RECOMMENDS──▶ Action:SlowDown
WearAnomaly ──RECOMMENDS──▶ Action:InspectJoints
ProtectiveStop ──TRIGGERED_BY──▶ CollisionRisk
```

---

## 11. 구현 계획

### Phase 1: 데이터 생성 (백엔드)
1. `src/simulation/scenario_sequencer.py` 생성 - 상태 머신
2. `src/simulation/ur5e_telemetry_generator.py` 생성 - UR5e 데이터 생성
3. `src/simulation/correlation_engine.py` 생성 - 상관관계 계산
4. 기존 센서 SSE에 통합

### Phase 2: API 확장 (백엔드)
1. `/api/sensors/correlation/stream` 엔드포인트 추가
2. 기존 `/api/sensors/stream` 확장

### Phase 3: 프론트엔드
1. `CorrelationTable` 컴포넌트 생성
2. `useCorrelationSSE` 훅 생성
3. `LiveView`에 통합

### Phase 4: 온톨로지 연결
1. 새로운 노드/엣지 타입 추가
2. 이벤트 발생 시 온톨로지 업데이트

---

## 12. 라벨링 주의사항

**합성 데이터임을 명확히 표시해야 함:**

```
UI에 표시:
┌─────────────────────────────────────────────────────────────┐
│ 🔬 이기종 상관분석 (Simulated Data)                          │
│ ※ UR5e 데이터는 시나리오 기반 시뮬레이션입니다                │
│ ※ 위험도 점수는 확률이 아닌 상대적 위험 지표입니다            │
└─────────────────────────────────────────────────────────────┘
```

---

## 변경 이력

| 날짜 | 버전 | 내용 |
|-----|------|-----|
| 2024-01-24 | 1.0 | 초기 설계 문서 작성 |
| 2024-01-24 | 1.1 | GPT 피드백 반영: 시나리오 기반 생성, 구간 유지, 상태 전이 규칙 추가, collision_probability → collision_risk_score 변경 |
| 2024-01-24 | 2.0 | **구현 완료** ✅ |

---

## 13. 구현 완료 내역 ✅

### 13.1 백엔드 (Phase 1-2) 완료

| 파일 | 설명 |
|-----|------|
| `src/simulation/__init__.py` | 시뮬레이션 모듈 초기화 |
| `src/simulation/scenario_sequencer.py` | 시나리오 상태 머신 (5개 시나리오, 가중치 기반 전환, 2초 보간) |
| `src/simulation/ur5e_generator.py` | UR5e 텔레메트리 생성기 (속도, 토크, 전류, 안전모드, 보호정지 래칭) |
| `src/simulation/correlation_engine.py` | Axia80+UR5e 상관분석 엔진 (위험도 계산, 권장 조치) |
| `src/api/routes/sensor.py` | 새 API 엔드포인트 추가 |
| `src/api/schemas/sensor.py` | 새 스키마 타입 추가 |

### 13.2 프론트엔드 (Phase 3) 완료

| 파일 | 설명 |
|-----|------|
| `frontend/src/types/api.ts` | IntegratedStreamData 등 TypeScript 타입 추가 |
| `frontend/src/hooks/useSSE.ts` | useIntegratedSSE 훅 추가 |
| `frontend/src/components/live/CorrelationTable.tsx` | 상관분석 테이블 컴포넌트 |
| `frontend/src/components/live/LiveView.tsx` | CorrelationTable 통합 |

### 13.3 새로운 API 엔드포인트

```
GET  /api/sensors/stream/integrated   - UR5e+Axia80 통합 SSE 스트림
POST /api/sensors/stream/scenario     - 시나리오 강제 전환 (테스트용)
GET  /api/sensors/stream/statistics   - 스트림 통계 조회
POST /api/sensors/stream/reset        - 스트림 리셋
```

### 13.4 테스트 결과

충돌 시나리오 시뮬레이션 테스트:
```
Phase 0-30%:  접근 단계 - speed ↑, force ↑, risk: low
Phase 40-50%: 접촉 순간 - force spike (107N→147N), protective_stop 발동
Phase 50-70%: 반응 단계 - force ↓, robot stopped
Phase 70-90%: 복구 단계 - force ↓, risk: low
```

### 13.5 미구현 (Phase 4)

- [ ] 온톨로지 그래프에 이벤트 노드/엣지 추가 (TelemetryWindow, ForceSpike 등)
- [ ] 이벤트 발생 시 온톨로지 자동 업데이트

### 13.6 시작 방법

```bash
# 백엔드 재시작 필요 (새 엔드포인트 활성화)
python -m uvicorn src.api.main:app --reload --host 0.0.0.0 --port 8000

# 프론트엔드
cd frontend && npm run dev
```

Live 탭에서 "UR5e + Axia80 상관분석" 카드 확인!
